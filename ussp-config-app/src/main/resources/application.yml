spring:
  application:
    name: ussp-config-app
  profiles:
    active: test
  rabbitmq:
    host: 10.110.1.240
    port: 5672
    username: admin
    password: admin
    virtual-host: /
  cloud:
    config:
      bus:
        trace:
          enabled: true

      server:
        git:
          uri: http://10.110.1.240/dev2/ussp.git
          search-paths: config-repo
          username: xiayutai@hongte.info
          password: 171717xia

security:
  user:
    name: root
    password: 123456


server:
  port: 8888

eureka:
  instance:
      instance-id: ${eureka.instance.hostname}:${server.port}
      lease-renewal-interval-in-seconds: 2 #契约更新间隔
      lease-expiration-duration-in-seconds: 4 #契约过期时间
      hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://localhost:11111/eureka/


management:
  security:
    enabled: false

